import tkinter as tk
from utils.common import search
from utils.config import library, root, searchFrame, currentFrame
from utils.ui.displaySearchFrame import displaySearchFrame

displaySearchFrame()

root.mainloop()
